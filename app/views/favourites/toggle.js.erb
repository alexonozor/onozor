$(".favourite-counter-<%= @question.id %>").text(<%= @question.favourites_count %>);
<% if @question.favourited?(current_user) %>
  $('#favorite-<%= @question.id %>').removeClass("yellow").addClass('yellow');
<% else %>
  $('#favorite-<%= @question.id %>').removeClass("yellow");
 <% end %>
