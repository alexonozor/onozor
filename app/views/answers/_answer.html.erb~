
          <div id="answer-body" data-time="<%= answer.created_at.to_i %>" >
          <%= div_for answer do %>
	     <div class="row">
		
              <div class="col-md-1 votes">

		   <% if current_user && current_user.can_vote?(answer) %>
<h4><%= link_to  (image_tag "arrow-up.png"), vote_answer_path(answer, value: 1), remote: true, method: "post" %></h4>
		             <h4 id='question_vote_count'><%= answer.votes %></h4>
<h4><%= link_to (image_tag "arrow-down.png"), vote_answer_path(answer, value: 1), remote: true, method: "post" %></h4>
		   <% else %>
 <h4><%= link_to  (image_tag "arrow-up.png"), vote_answer_path(answer, value: 1), remote: true, method: "post" %></h4>
		               <h4 id='question_vote_count'><%=  answer.votes %></h4>
<h4><%= link_to (image_tag "arrow-down.png"), vote_answer_path(answer, value: 1), remote: true, method: "post" %></h4>
		   <% end %>
                </div>


            <div class="col-md-10 body-rigion">
             <span class="answer-format"><%= markdown(answer.body).html_safe %>
              <p><small class="text-muted"><i class="glyphicon glyphicon-time"></i> 
               <%= distance_of_time_in_words_to_now answer.created_at %> ago
                <%= link_to(t("Add comment"), new_comment_path(:comment => { :commentable_type => answer.class.name,
                                                              :commentable_id => answer.id}),
                                                              :remote => true,
                                                              :class => "btn") %>
						      	      </small>
							     </p>
							    </span>
							   </div>

		    <div class="col-md-1">
		      <nav class="offset-2">
		         <small class="mute">
                          <% if current_user.present? %>
			    <% if current_user && answer.user_id == current_user.id || current_user.admin?  %> 
<i id="edit"><%= link_to "<i class='glyphicon glyphicon-pencil'></i>".html_safe , edit_answer_path(answer.id), :remote => true   %></i>
<i id="delete"><%= link_to "<i class='glyphicon glyphicon-trash'></i>".html_safe , answer , :method => :delete, :remote=> true %></i>
			     <% end %>
                           <% end %>
		          </small>
			 </nav>			 		      
                        <%= link_to( image_tag(answer.user.avatar.url(:thumb), 
                                           class:"follow-pix", :border =>"0"), 
                                           user_path(answer.user)) %>
                                           <%= link_to  answer.user.username, 
                                            user_path(answer.user) %>
                                  			                             
					   </div>
                                     <div class="comment row" id="comments">
				       <div class="col-md-1"></div>
                                      <div class="col-md-11">
                                     <table>
                                    <%= render answer.comments %>
                                     </table> 
                                      </div> 
                                       </div>
				          </div>
				         <% end %>
                                        </div>
                                       
                                   
                                 
                             
				 
         
					  
                                    
                                          

                                         
      
   

    
                                          

 











