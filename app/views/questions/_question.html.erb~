

<div class="index-question question-body" data-question="<%= question.created_at.to_i %>" data-id="<%= question.id %>">
      
         <div class="vote">
           <p><%= (pluralize (question.votes), "Vote") %></p>
         </div>
          <div class="answer-box" style="background-color: <%= question.answer_count_color %>;"><p><%= (pluralize (question.answers.size ), "Answer")   %></p></div>
          <div class="views">
            <p><%= (pluralize (question.views), "View") %></p>
          

        <div class="question-headlines">
          <h4><%=link_to question.name, question %></h4>
          <div class="tag-index">

            <%= raw question.tag_list.map { |t| link_to t, tag_path(t) }.join('') %>
          </div>

          <div class="asker-details"><p><small><%= time_ago_in_words question.created_at %> ago <%=link_to question.user.username,
                                                                                                user_path(question.user.slug)  %></small> </p></div>
        </div>
      </div>
    </div>




