
  <div class="container">
    <div class="navbar-header">
      <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target="#bs-navbar" aria-controls="bs-navbar" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%#= link_to image_tag('logo.png'), root_path, class:'navbar-brand' %>
    </div>
    <nav id="bs-navbar" class="collapse navbar-collapse">
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group has-success has-feedback">
          <label class="control-label sr-only" for="inputSuccess5">Hidden label</label>
          <input type="text" class="form-control" id="inputInfo5" placeholder=" Serach Questions" aria-describedby="inputSuccess5Status">
          <span class="fa fa-search form-control-feedback" aria-hidden="true"></span>
          <span id="inputSuccess5Status" class="sr-only">(success)</span>
        </div>
      </form>
      <%= link_to "Start new Topic", question_advise_path, class: 'btn  navbar-btn offset-2 topic-button'  %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <p class="navbar-text">Hi, <%= current_user.username.titleize if current_user %></p>
            <li><%= link_to image_tag current_user.avatar.url(:thumb) if current_user.avatar.present?  %></li>
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
        <% else %>
            <li><%= link_to "Signup", new_user_registration_path %></li>
             <li><%= link_to "Login", new_user_session_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>



