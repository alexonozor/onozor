 <table class="table table-condensed">
<% for f in @users %>
  <tr>
 <td><p><strong><%=link_to f.fullname, f %></strong></p> </td>
 <td><small> since <%=distance_of_time_in_words_to_now f.created_at %> ago </small></td>
 <td>
 <%= form_for(f.relationships(followed_id: f.id), remote: true, html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-primary btn-sm btn-danger" %>
<% end %>
</td>
  </tr>
 <% end %>
  </table>
