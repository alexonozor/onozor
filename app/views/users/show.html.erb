<% title "#{@user.username} Profile" %>
<% content "#{@user.bio}" %>
<div class="container-set">
  <div class="row clear-top">
    <div class="col-md-3">
      <div class="profile-picture-image">
       <%= image_tag @user.avatar.url(:medium), class:"profile-picture", class: 'img-circle' %>
      </div>
      <div class="list-group">
        <a href="#" class="list-group-item disabled">Feeds</a>
        <%= link_to (pluralize @user.questions.count, "Question"), user_path(@user) + "/questions", :remote => true , class: "list-group-item"  %>
        <%= link_to (pluralize @user.answers.count, "Answer"), user_path(@user) + "/answers", :remote => true , class: "list-group-item"  %>
        <%= link_to (pluralize @user.followers.count, "Follower"), user_path(@user) + "/followers", :remote => true ,  class: "list-group-item"  %>
        <%= link_to (pluralize @user.followed_users.count, "Following"), user_path(@user) + "/following", :remote => true , class: "list-group-item"  %>
        <%= link_to (pluralize @user.favourites.count, "Favorites"), user_path(@user) + "/favorites", :remote => true , class: "list-group-item"  %>
      </div>
    </div>
   <div class="col-md-6">
     <div class="info-text center_col">
       <% if @current_user && @user.id == @current_user.id %>
        <h1 class="profile-picture-name">
          <%= render partial: "user_info", locals: {option: "fullname"} %>
        </h1>
        <%= render partial: "user_info", locals: {option: "intrest"} %>
        <%= render partial: "user_info", locals: {option: "bio"} %>
        <% else %>
          <h1 class="profile-picture-name"><%= @user.fullname %></h1>
          <h3 class="user_intrest"><%= @user.intrest || "<i>No interests yet</i>".html_safe %></h3>
          <p><%= (@user.bio && !@user.bio.empty?) ? @user.bio : "<i>No bio yet</i>".html_safe %></p>
        <% end %>
       <div id="user-contact-info">
         <ul>
           <li><%= render 'follow_form' if signed_in?  %></li>
           <li class="user_action">
             <%= render "message_user" %>
           </li>
         </ul>
       </div>

     </div>


     <div id="feeds">
      <%=render "users/all_feeds" %>
     </div>
   </div>
   <div class="col-md-3">
     <div class="sidebar_right">
       <%=link_to user_path(@user), class: "views_on_profile" do %>
        <h3><span class="users_answers"><%= "Views on #{@user.first_name}'s Answers" %></span></h3>
        <div class="stat">
          <span class="total_count"><%= @user.views %></span>
          <div class="stat_label is_increase"> Last 30 Days</div>
        </div>
        <div class="stat">
          <span class="total_count"><%= @user.views %></span>
          <div class="stat_label">All Time</div>
        </div>
       <% end %>
       <div>
          <div class="recent">Recent Posts</div>
        <% @related_questions.each do |a| %>
         <p class="similar"><%= link_to a.name, a  %></p>
        <% end %>
      </div>
     </div>
  </div>
</div>
</div>
