<%= form_for(current_user.relationships.find_by(followed_id: @user.id), remote: true, html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow | #{@user.followers.count}", class: "btn btn-danger btn-sm" %>
<% end %>
